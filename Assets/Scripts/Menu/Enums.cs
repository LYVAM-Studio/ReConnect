namespace Reconnect.Menu
{
    public enum MenuState { None, Main, Singleplayer, Multiplayer, Settings, Pause, BreadBoard, ConnectionFailed, Lessons, ImageViewer, KnockOut, Quit }
    public enum CursorState { Shown, Locked }
    public enum PlayMode { Single, MultiHost, MultiServer }
}